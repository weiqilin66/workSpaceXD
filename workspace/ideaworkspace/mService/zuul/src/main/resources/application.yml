eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
server:
  port: 1111  #6开始端口无法使用 😳 待解
spring:
  application:
    name: service-zuul
zuul:
  routes:
    api-ribbon:
      path: /api-ribbon/**
      serviceId: service-ribbon   #/service-ribbon/** 路径也可以访问到

    api-feign:
      path: /api-feign/**
      serviceId: service-feign

    api-vhr:
      path: /api-vhr/**
      serviceId: hr
